$kind: http-request
name: Login
method: POST
url: "{{base_url}}/api/auth/login"
headers:
  - key: Content-Type
    value: application/json
body:
  mode: raw
  raw: |
    {
      "email": "usuario@wbm.com",
      "password": "{{password}}"
    }
  options:
    raw:
      language: json
